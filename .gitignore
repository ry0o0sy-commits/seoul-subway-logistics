# ─────────────────────────────────────────────────────────
# 원본 데이터 — 저장소에 올리지 않는다.
#   특히 택배 물동량은 서울시 빅데이터캠퍼스 폐쇄형 환경에서 반출 절차를
#   거쳐 취득한 자료라 외부 공개 대상이 아니다. README의 데이터 출처 표 참고.
# ─────────────────────────────────────────────────────────
*.xls
*.xlsx
*.shp
*.shx
*.dbf
*.prj
*.cpg

# 입력 CSV(공공데이터 원본)는 저장소 루트에 놓고 쓴다 — 추적하지 않는다.
# output_data/ 아래 결과 CSV는 아래 예외 규칙으로 추적한다.
/*.csv

# ─────────────────────────────────────────────────────────
# 대용량 캐시 — step1 계열이 생성한다
# ─────────────────────────────────────────────────────────
*.npy
*.graphml

# 이전 버전 결과 백업·중간 격자 — 어느 깊이에 있든 제외
backup_*/
panel_grid_*/

# step1이 만드는 중간 산출물(공공데이터 가공) — 결과가 아니므로 제외
output_data/stations.csv
output_data/dongs.csv

# ─────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# CBC 솔버 임시 파일
*.lp
*.mps
*.sol
